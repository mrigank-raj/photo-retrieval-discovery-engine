# Everything needed to run the full pipeline locally (collect, classify, extract, cluster, tests). Use: pip install -r requirements-pipeline.txt
-r requirements.txt
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.0
google-play-scraper>=1.2
playwright>=1.45        # Google Photos Community listing pages; then run: python -m playwright install chromium
scikit-learn>=1.5
sentence-transformers>=3.0   # pulls in torch; first run downloads the all-MiniLM-L6-v2 model (about 90 MB)
pytest>=8.0
