# Copy to .env and fill in. .env is git-ignored. Everything here is a free tier; no billing is ever needed.
GEMINI_API_KEY=
APIFY_TOKEN=
YOUTUBE_API_KEY=
# Random secret used to hash usernames (never store raw usernames). Generate once; do not change mid-project.
AUTHOR_HASH_SALT=
GROQ_API_KEY=
